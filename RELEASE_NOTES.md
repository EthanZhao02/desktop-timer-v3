## 智域计时 v1.1.4

### 宠物窗口稳定性
- 修复宠物窗口偶发被拉成横跨桌面的长条问题
- 启动、面板关闭及运行期尺寸变化时自动恢复合法窗口尺寸
- 将“下一个闹钟”提示限制为固定小气泡，避免随窗口异常拉伸

### 宠物拖动
- 移除原生拖动与 JavaScript 拖动并存造成的事件冲突
- 宠物身体区域统一保持可交互，不再因透明像素或骨骼动画出现拖动死区
- 使用屏幕坐标计算拖动位置，减少移动过程中的卡顿和回弹
- 手动拖动位置限制在当前显示器工作区内，避免宠物被拖出屏幕

---

## 智域计时 v1.1.3

### 发布包修复
- Windows Release 同时提供安装版与便携版，并使用清晰的英文文件名
- Apple Silicon 提供 DMG 与 ZIP，Intel Mac 提供更稳定的 ZIP 内测包
- 应用功能与 v1.1.2 一致

---

## 智域计时 v1.1.2

### macOS 内测版
- GitHub Actions 同时生成 Apple Silicon 与 Intel Mac 的 DMG、ZIP 包
- 核心计时、闹钟、自定义铃声、桌面宠物和真人眨眼验证可在 macOS 使用
- macOS 可在闹钟结束后自动启动已安装的汽水音乐，也可选择其他 `.app` 音乐应用
- 媒体键控制及汽水音乐场景自动切换暂为 Windows 专属；Mac 版只负责可靠启动应用
- 当前内测包尚未进行 Apple Developer 签名与公证，首次启动需在“隐私与安全性”中手动允许

### 闹钟真人验证
- 每个闹钟可单独开启“停止闹钟需要拍照验证”
- 使用本地 MediaPipe 人脸关键点模型，检测到人脸并完成一次眨眼后才能停止
- 摄像头画面仅在内存中处理，不保存、不上传
- 摄像头不可用时提供随机验证码备用验证
- 开启验证后铃声会持续播放，直到验证成功

### 音乐与宠物运动
- 明确标注场景模式仅对汽水音乐有效，其他音乐应用不再显示为可切换场景
- 音乐设置增加“测试启动与播放”，并显示汽水场景切换是否真正成功
- 宠物自动运动增加混合、桌面行走、原地踏步三种模式
- 混合模式会自动原地踏步，跑步仍保持桌面移动和撞墙转向
- 重新校准汽水音乐 3.7.0 场景卡片位置，并改为单击切换
- 修复其他音乐应用已运行时没有被唤醒到前台的问题
- 支持恢复 EthanMusic 等应用的隐藏主窗口，并将播放指令发送给目标窗口

---

## 智域计时 v1.1.1

### Windows 图标修复
- 修复任务栏窗口显示空白文件图标的问题
- 显式设置 AppUserModelID，使任务栏分组与安装信息保持一致
- 主窗口和宠物窗口统一使用打包后的 ICO 图标资源

---

## 智域计时 v1.0.5

### macOS 支持
- 新增 Apple Silicon（M1/M2/M3/M4/M5）原生构建
- 新增 Intel Mac x64 构建
- 同时提供 DMG 安装镜像与 ZIP 便携包
- macOS 保留计时、闹钟、桌面宠物、托盘、锁屏事件和聊天功能
- Windows 专属的前台窗口姿态识别在 macOS 上自动停用

### 发布说明
- Windows 与 macOS 安装包均由 GitHub Actions 从同一标签构建
- macOS 包暂未使用 Apple Developer 签名和公证，首次打开需在系统隐私与安全设置中允许

---

## 智域计时 v1.0.4

### 桌面宠物优化
- 常驻宠物窗口从 280x340 缩小为 220x280，更接近轻量桌宠比例
- 角色显示区从 160x200 缩小为 124x158，并同步收紧气泡、状态点和时间条
- 聊天或设置面板打开时自动扩展到 440x600，关闭后恢复紧凑尺寸
- 面板扩展和收起时保持当前屏幕右下角锚定，兼容多显示器工作区

### 修复与验证
- 修复聊天与设置面板叠加时窗口可能提前缩小的问题
- 聊天面板改为不透出底层宠物的实色背景
- 更新 UI 截图测试和打包校验，覆盖常驻窗口与展开面板尺寸
- Windows x64 便携版已通过 11 项逻辑测试和包内源码哈希校验

### 下载说明
- 文件：`智域计时 1.0.4.exe`
- SHA-256：`194E78A44A8F4274F163B2399363FAA07B24CAFA16E301E015AE0DC65701F44A`
- 当前安装包未使用代码签名，Windows SmartScreen 可能显示未知发布者提示

---

## 🐱 智域计时 v1.0.2 更新

### ✨ 新功能
- **🔒 锁屏关怀**：锁屏时说晚安，解锁时说欢迎回来（带睡眠时长）
- **📊 实时姿态检测**：自动识别活动窗口，切换12种宠物姿态
- **📱 窗口感知**：VSCode→编程 / 微信→摸鱼 / 浏览器→浏览 / 锁屏→睡觉
- **💬 星野对话**：点击宠物直接和 AI 助手对话（需 QClaw 星野运行中）

### 🐛 修复
- 闹钟卡片 box-shadow 残留 bug
- 宠物窗口关闭按钮脱离问题

### 🧹 优化
- 清理 1GB+ 调试文件，项目结构更清晰
- 全新推广向 README（徽章/分享按钮/平台说明）
- GitHub Actions 自动构建工作流

### 📦 使用方法
下载 exe 后双击运行，无需安装，Windows 即开即用。

> 💬 星野对话需要 QClaw 助手正在运行（localhost:53717 Gateway），宠物会自动连接。
# 智域计时 1.1.0（2026-09-19）

- 主窗口升级为统一的桌面效率工具界面，倒计时、正计时、闹钟和设置使用一致的组件样式。
- 移除固定考试与节日预设，倒计时完全由用户自定义。
- 宠物状态与正计时、倒计时联动，支持工作、暂停和完成庆祝反馈。
- 新增今日专注时长与完成次数统计。
- 闹钟支持直接编辑，并增强系统休眠、解锁后的补触发检查。
- 新增宠物自动活动和始终置顶设置。
- 新增运行日志、版本信息及数据目录快捷入口。
- Windows 同时提供安装版和便携版，安装版支持开始菜单、桌面快捷方式和标准卸载。
